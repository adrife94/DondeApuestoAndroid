<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.dam.aed.nuevo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EleccionCasaSportiumActivity"></activity>
        <activity android:name=".EleccionPremierActivity" />
        <activity android:name=".Casas" />
        <activity android:name=".CasaSelecionada3" />
        <activity android:name=".CasaSelecionada2" />
        <activity android:name=".CasaSelecionada1" />
        <activity android:name=".Opciones" />
        <activity android:name=".CalculadoraActivity" />
        <activity android:name=".EleccionLaLigaActivity" />
        <activity android:name=".Eleccion" />
        <activity android:name=".Tabla" />
        <activity
            android:name=".SS"
            android:label="@string/title_activity_ss"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>